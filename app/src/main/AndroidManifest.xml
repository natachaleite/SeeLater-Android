<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.formobile.seelater">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_seelater"
        android:label="SeeLater"
        android:roundIcon="@drawable/logo_seelater"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <activity android:name=".InicialActivity" />
        <activity android:name=".AdicionaLivrosActivity" />
        <activity android:name=".ExibeLivrosActivity" android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".AdicionaFilmesActivity" />
        <activity android:name=".ExibeFilmesActivity" android:theme="@style/AppThemeNoActionBar"/>
        <activity android:name=".AdicionaSeriesActivity" />
        <activity android:name=".ExibeSeriesActivity" android:theme="@style/AppThemeNoActionBar"/>

        <!-- <activity -->
        <!-- android:name=".MenuLateral" -->
        <!-- android:label="@string/title_activity_menu_lateral" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->


    </application>

</manifest>